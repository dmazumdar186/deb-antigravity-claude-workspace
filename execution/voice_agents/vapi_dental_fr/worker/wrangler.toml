# account_id is auto-detected from `wrangler whoami` — leave blank.
# Secrets are set via `wrangler secret put` (or `wrangler secret bulk`) — never committed.

name = "vapi-dental-fr"
main = "src/index.ts"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]
account_id = ""

[vars]
APP_VERSION = "0.6.0"
CLINIC_NAME = "Cabinet Dentylis"
CLINIC_PHONE = "01 85 00 57 05"
DEMO_MODE = "true"

[observability.logs]
enabled = true
