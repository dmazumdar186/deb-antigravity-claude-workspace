# Fetch cache and run artifacts. Regenerable, large, and full of third-party
# page content -- the pipeline is resumable from them but they are not source.
run/_httpcache/
run/_probe_*.json
run/gaia-*/
__pycache__/
*.pyc

# Audit trails and secrets-adjacent operator logs (recruit_crm_audit.jsonl,
# outreach_audit.jsonl, optout.jsonl, spend_ledger.jsonl, ...). Contains real
# candidate PII and CRM payloads -- never source, never committed.
logs/
