node_modules
dist
.astro
.env
.env.local
.env.production
.DS_Store
*.log

# Reviews build artifact — regenerated by scripts/sync_reviews.mjs before
# each build from the KV public read endpoint. Source of truth is KV; this
# file is what Testimonials.astro reads at build time. Committed only when
# KV is unreachable (bootstrap fallback).
src/content/reviews.json
