# Scoped requirements for execution/personal_workflows/prodcraft_medspa (P).
#
# Generated by grepping third-party imports across P/**/*.py (2026-09-16, round-2 fixops item 4).
# Versions kept consistent with the root requirements.txt where the same package appears there.
# Stdlib-only imports (os, json, re, pathlib, subprocess, fcntl, msvcrt, ...) and this package's
# own local modules (execution.*, preview.*) are not listed here.
#
# Regenerate the import list with:
#   python3 -c "
#   import ast, pathlib
#   root = pathlib.Path('execution/personal_workflows/prodcraft_medspa')
#   mods = set()
#   for f in root.rglob('*.py'):
#       if '__pycache__' in f.parts: continue
#       tree = ast.parse(f.read_text(encoding='utf-8'), filename=str(f))
#       for node in ast.walk(tree):
#           if isinstance(node, ast.Import):
#               mods.update(a.name.split('.')[0] for a in node.names)
#           elif isinstance(node, ast.ImportFrom) and node.level == 0 and node.module:
#               mods.add(node.module.split('.')[0])
#   print(sorted(mods))
#   "

# LLM calls (audit vision/scoring, extract_services, draft_email, lint_draft, doctor --live)
anthropic>=0.40.0

# .env loading (common/config.py bootstrap())
python-dotenv>=1.0.0

# Image handling (audit screenshots / template hero image processing)
Pillow>=10.0.0

# HTTP calls (Google Places, PageSpeed, Apollo, Findymail, Hunter, MillionVerifier, Cloudflare/R2
# SigV4 calls, Telegram, Supabase REST)
requests>=2.31.0

# Gmail API (outreach send/scan_replies, doctor.py --live gmail check) + OAuth token handling
google-api-python-client>=2.0
google-auth-oauthlib>=1.0

# Google Sheets mirror (scripts/sync_sheets.py)
gspread>=6.0

# scripts/sync_sheets.py --xlsx optional local workbook export
openpyxl>=3.1.0

# Playwright Chromium (audit screenshots + acceptance_template.py + preview_gate.py's acceptance
# re-run). Browsers themselves are installed separately via
# `python -m playwright install --with-deps chromium` (see the daily/replies workflows) —
# this pin only gets the Python driver.
playwright>=1.45

# Optional, degrade-gracefully imports: both call sites already fall back cleanly when the
# package is absent (doctor.py's SPF/DMARC TXT lookup falls back to `nslookup`; db/apply_schema.py
# falls back to printing the SQL for manual paste), but the daily/replies workflows should still
# install them so the live path is exercised instead of silently degrading in production.
dnspython>=2.0.0
psycopg[binary]>=3.1
cffi>=1.17
