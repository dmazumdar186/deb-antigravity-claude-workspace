# Execution Registry

This file is auto-generated by `generate_registry.py`. Do not edit manually.
Run `python execution/generate_registry.py` after adding or modifying any script.
Last updated: 2026-09-16

## Scripts

| Script | Description | Inputs | Outputs |
|--------|-------------|--------|---------|
| `_TEMPLATE.py` | — | — | — |
| `_TEMPLATE_autoresearch.py` | — | — | — |
| `content/ad_creative_batch.py` | Renders a batch of square ad variants from a brand profile.json | --brand-dir <dir containing profile.json + copy_pool.json> | <brand-dir>/variants/variant_NN.html, <brand-dir>/renders/ |
| `content/humanizer.py` | Strip AI-tells from text and rewrite it in a personal voice profile. | — | — |
| `content/rosy_origami/composer.py` | — | — | — |
| `content/rosy_origami/fetchers/ig_instaloader.py` | — | — | — |
| `content/rosy_origami/generate_demo.py` | — | — | — |
| `custom_scrapers/adzuna_jobs.py` | Adzuna jobs API scraper. Free-tier: 250 req/day per app. Covers FR, DE, NL, ES, IT, BE, AT, PL, PT, CA, US among others. Returns structured fields incl. contract_time (permanent/contract) and full_time/part_time flags. | queries (list[str]) — title synonyms; country (str) — ISO2 like "fr"; max_results (int) default 50; results_per_page default 50; pages default 1. Env vars: ADZUNA_APP_ID, ADZUNA_APP_KEY. | list[RawJob] dicts written to .tmp/job_search/<run_id>/raw_adzuna_<country>.json (also returned). |
| `custom_scrapers/apec_jobs.py` | Scrape PM/PO job listings from APEC (cadres/executives France) via Firecrawl SDK. | — | — |
| `custom_scrapers/france_travail_jobs.py` | Fetch PM/PO job listings from France Travail (Pôle Emploi) official REST API using OAuth2 client_credentials flow. | — | — |
| `custom_scrapers/google_jobs_serper.py` | Scrape PM/PO job listings from Google Jobs via Serper.dev /jobs endpoint. | — | — |
| `custom_scrapers/indeed_jobs.py` | Scrape PM/PO job listings from Indeed France via Firecrawl SDK with stealth proxy. | — | — |
| `custom_scrapers/job_filter.py` | Pure filter for French PM/PO job listings. Normalizes titles and companies, detects language, applies include/exclude keyword rules, and annotates each job with pass/fail status. No API calls, no DB access. | List of RawJob dicts (JSON file via --input) and config/job_tracker.json (loaded automatically). | List of CandidateJob dicts written to --output JSON file. |
| `custom_scrapers/jooble_jobs.py` | Jooble jobs API scraper. Free-tier via form signup (email-based, no card). POST endpoint. Returns structured 'type' field for contract classification. Covers all major countries by full-name string. | queries (list[str]) — title synonyms; country (str) — Jooble full country name (e.g. "France"); page (int) default 1; max_results (int) default 50. Env var: JOOBLE_API_KEY. | list[RawJob] dicts written to .tmp/job_search/<run_id>/raw_jooble_<iso2>.json (also returned). |
| `custom_scrapers/wttj_jobs.py` | Scrape PM/PO job listings from Welcome to the Jungle (France only) via Firecrawl SDK. | — | — |
| `enrichment/anymailfinder_lookup.py` | Find business owner emails via AnymailFinder API. | --input, --output, --min-confidence, --mock; env: ANYMAILFINDER_API_KEY | .tmp/enriched_leads.json |
| `enrichment/firecrawl_linkedin_dork.py` | Find LinkedIn profile URLs of product/HR contacts at a company using Firecrawl Google dorks (no direct LinkedIn scraping). | — | — |
| `enrichment/million_verifier.py` | Verify email deliverability via Million Verifier API. | --input, --output, --accept, --mock; env: MILLION_VERIFIER_API_KEY | .tmp/verified_leads.json |
| `google/gmail_send_digest.py` | Send an HTML email digest via Gmail SMTP using an App Password. Self-contained; does not import from execution/modules. | --html (path to HTML file), --subject (email subject), --recipient (email address), --dry-run (flag); env vars GMAIL_SMTP_USER, GMAIL_SMTP_APP_PASSWORD, JOB_TRACKER_RECIPIENT. | Email sent via SMTP; stdout confirmation or error message; notifications_log row written (via send_digest_and_log helper). |
| `google/google_sheets_writer.py` | Google Sheets writer for the Job Search pipeline. Service-account auth via gspread. Owns workbook bootstrap, batch writes, hidden column management, and the hidden _meta tab used for cron idempotency. | Env var GOOGLE_SERVICE_ACCOUNT_PATH (path to JSON key); SHEETS_SPREADSHEET_ID (the workbook). | Reads/writes the live Google Sheet. Returns Python dicts (read) or void (write). |
| `gtm_client_workflows/accessory_masters_pipeline.py` | End-to-end orchestration pipeline for Accessory Masters cold email system. | --config, --stage, --mock, --force, --poll-replies, --weekly-report; env: multiple API keys | .tmp/pipeline_state.json, .tmp/personalized_leads.json |
| `gtm_client_workflows/gaia_sourcing/core/cache.py` | — | — | — |
| `gtm_client_workflows/gaia_sourcing/core/config.py` | — | — | — |
| `gtm_client_workflows/gaia_sourcing/core/contracts.py` | — | — | — |
| `gtm_client_workflows/gaia_sourcing/core/ocr.py` | — | — | — |
| `gtm_client_workflows/gaia_sourcing/core/providers.py` | — | — | — |
| `gtm_client_workflows/gaia_sourcing/layers/adversarial.py` | — | — | — |
| `gtm_client_workflows/gaia_sourcing/layers/contact.py` | — | — | — |
| `gtm_client_workflows/gaia_sourcing/layers/extract.py` | — | — | — |
| `gtm_client_workflows/gaia_sourcing/layers/gates.py` | — | — | — |
| `gtm_client_workflows/gaia_sourcing/layers/linkcheck.py` | — | — | — |
| `gtm_client_workflows/gaia_sourcing/layers/messages.py` | — | — | — |
| `gtm_client_workflows/gaia_sourcing/layers/movability.py` | — | — | — |
| `gtm_client_workflows/gaia_sourcing/layers/validator.py` | — | — | — |
| `gtm_client_workflows/gaia_sourcing/render/render.py` | — | — | — |
| `gtm_client_workflows/gaia_sourcing/roles.py` | — | — | — |
| `gtm_client_workflows/gaia_sourcing/run.py` | — | — | — |
| `gtm_client_workflows/gaia_sourcing/sources/acp.py` | — | — | — |
| `gtm_client_workflows/gaia_sourcing/sources/company_bios.py` | — | — | — |
| `gtm_client_workflows/gaia_sourcing/sources/linkedin_lookup.py` | — | — | — |
| `gtm_client_workflows/gaia_sourcing/sources/oral_hearing_web.py` | — | — | — |
| `gtm_client_workflows/gaia_sourcing/sources/technical_evidence.py` | — | — | — |
| `gtm_client_workflows/gaia_sourcing/tests/acceptance_gaia.py` | — | — | — |
| `gtm_client_workflows/gaia_sourcing/tests/front_door_gaia.py` | — | — | — |
| `gtm_client_workflows/gaia_sourcing/tests/test_cost_ceiling.py` | — | — | — |
| `gtm_client_workflows/gaia_sourcing/tests/test_extract_and_messages.py` | — | — | — |
| `gtm_client_workflows/gaia_sourcing/tests/test_gates_and_validator.py` | — | — | — |
| `gtm_client_workflows/gaia_sourcing/tests/test_gates_seniority_and_links.py` | — | — | — |
| `gtm_client_workflows/gaia_sourcing/tests/test_identity.py` | — | — | — |
| `gtm_client_workflows/gaia_sourcing/tests/test_layers_llm_paths.py` | — | — | — |
| `gtm_client_workflows/gaia_sourcing/tests/test_ocr.py` | — | — | — |
| `gtm_client_workflows/gaia_sourcing/tests/test_orchestrator.py` | — | — | — |
| `gtm_client_workflows/gaia_sourcing/tests/test_pipeline.py` | — | — | — |
| `gtm_client_workflows/gaia_sourcing/tests/test_providers_and_cache.py` | — | — | — |
| `gtm_client_workflows/gaia_sourcing/tests/test_render.py` | — | — | — |
| `gtm_client_workflows/gaia_sourcing/tests/test_sources.py` | — | — | — |
| `gtm_client_workflows/gaia_sourcing/tests/test_stages.py` | — | — | — |
| `gtm_client_workflows/generate_qa_test_plan.py` | Generates a professional XLSX QA test plan for the Accessory Masters | --output (default .tmp/accessory_masters_qa_test_plan.xlsx) | XLSX file at the specified path |
| `gtm_client_workflows/import_leads.py` | Flexible CSV lead importer for the Accessory Masters pipeline. | --csv (required), --mapping, --config, --upload, --mock, --output | .tmp/imported_leads.json (default), optional Instantly upload |
| `image_generation/prodcraft_thumbnail.py` | Generate a 1280x720 ProdCraft YouTube thumbnail from a title + optional subtitle. $0 path — pure Pillow, no API. Matches Living PRD bookend brand (dark navy bg, teal accent rail, big white sans-serif title). | — | — |
| `infrastructure/freshness_monitor.py` | Reads workspace_heartbeat/manifest.json and reports git-log + run-log freshness per project. Detects Pattern B (shipped claims surviving month-long silence) locally. No LLM calls, no network calls — pure git + filesystem. | --manifest (default: execution/infrastructure/workspace_heartbeat/manifest.json), --level {info,warn,fail}, --format {json,markdown,human}. Env: none. | JSON/Markdown/human report to stdout. Exit code 0 (all fresh), 1 (any DEGRADED at --level=warn/fail), 2 (manifest error). |
| `infrastructure/instantly_bounce_analysis.py` | — | CLI: --leads PATH (a leads_raw.json snapshot from instantly_guard's | JSON artifact + a human table on stdout. Exit 0 always (measurement, |
| `infrastructure/instantly_guard.py` | — | CLI: campaign_id (positional); --no-dry-run (default is DRY RUN), | JSON summary on stdout; dated log at |
| `infrastructure/migrate_local_model_pins.py` | One-shot local follow-up of the 2026-09-01 Fable 5.1 migration. On the | No CLI args. Env: OS / USERPROFILE (Windows detection), HOME. Reads the two | Rewrites the files above (only while the pin still says claude-fable-5); |
| `infrastructure/print_env_for_cloud.py` | — | — | — |
| `infrastructure/scan_transcript_attachments.py` | Find Claude Code sessions killed by an oversized attached file ("Prompt is too long"). | — | — |
| `infrastructure/setup_instantly_webhook.py` | Provision/rotate Instantly v2 webhook + Cloudflare INSTANTLY_WEBHOOK_SECRET so reply_received events POST to the worker with X-Webhook-Secret auth. | env: INSTANTLY_API_KEY (.env), optional INSTANTLY_WEBHOOK_SECRET_OVERRIDE; CLI: --list, --dry-run, --worker-url, --no-cloudflare | Writes Cloudflare secret via `npx wrangler secret put`; calls Instantly v2 webhooks API (list/delete/create); prints masked secret + new webhook id. |
| `infrastructure/token_usage_report.py` | — | — | — |
| `infrastructure/weekly_hardening_report.py` | Regenerates the workspace-level HARDENING report. Runs SAST, freshness_monitor, samples recent commits for shipped-claim phrasing, and writes HARDENING_BACKLOG_WORKSPACE_<YYYY-MM-DD>.md at the workspace root. Prior reports stay as an audit trail. No LLM calls. | --out (default: workspace root), --commits (default: 200), --since-days (default: 14). Env: none. | A single Markdown file HARDENING_BACKLOG_WORKSPACE_<date>.md and a stdout summary. Exit code 0 (report generated), 1 (any critical section failed), 2 (workspace root not detected). |
| `infrastructure/workspace_heartbeat/inline_manifest.py` | Reads manifest.json and inlines it into wrangler.toml's MANIFEST_JSON var so the Worker sees the current project list at deploy time. Idempotent; safe to run repeatedly. No network calls; no LLM calls. | manifest.json (adjacent to this script), wrangler.toml (adjacent). | Rewrites wrangler.toml with the MANIFEST_JSON var updated to the stringified manifest. |
| `infrastructure/workspace_sast.py` | — | — | — |
| `lead_sourcing/prospeo_leads.py` | Source B2B leads via Prospeo API for niches not indexed on Google Maps. | --domain, --company, --config, --output, --mock; env: PROSPEO_API_KEY | .tmp/prospeo_leads.json |
| `lead_sourcing/serper_maps_scraper.py` | Scrape Google Maps business listings via Serper.dev Maps API. | --query or --config, --limit, --output, --mock; env: SERPER_API_KEY | .tmp/serper_leads.json |
| `lead_sourcing/sirene_company_lookup.py` | Resolve a French company name to an INSEE SIRENE record and confirm digital-sector classification by NAF code. | — | — |
| `lead_sourcing/split_leads_by_geography.py` | Split a scraped-leads CSV into per-geography files and inject a personalization line column. | — | — |
| `mobile_apps/app_store_research.py` | Firecrawl wrapper for App Store Optimization (ASO) research. Searches the App Store or Play Store for a keyword (or a single competitor+store pair), scrapes the top-10 listings via Firecrawl's REST API with markdown format, parses out app name + rating + description, and returns the result as JSON on stdout. Supports parallel fan-out via ThreadPoolExecutor for multi-competitor batch runs. | CLI: --query "<keyword>", --store <appstore|playstore>, --limit <int> (default 10), --max-workers <int> (default 8); OR --competitors-file <csv_path> --stores ios android for batch mode; env: FIRECRAWL_API_KEY | JSON array of {name, rating, description, url} to stdout (single-query mode), or JSON {results: [...], scraped_count, skipped_count} (batch mode) |
| `mobile_apps/bootstrap_mobile_app.py` | Clone C:/Users/deban/dev/mobile-apps/_template into C:/Users/deban/dev/mobile-apps/{slug}, replace slug placeholders, run `git init`, append an entry to registry.json. Also supports --remove with git-dirty / unpushed-commits guards. | CLI: slug (positional, kebab-case), --dry-run, --force, --remove <slug>, --force-remove | New repo dir at C:/Users/deban/dev/mobile-apps/{slug}; mutated execution/mobile_apps/registry.json |
| `mobile_apps/eas_build_helper.py` | Wrap `eas build --platform <ios|android> --profile <preview|production>`, capture stdout/stderr (utf-8), parse build ID + URL from EAS output, optionally POST a JSON status to MOBILE_BUILD_WEBHOOK_URL, and update last_build_sha in registry.json for the given app. | CLI: --app <slug>, --platform <ios|android>, --profile <preview|production>; env: MOBILE_BUILD_WEBHOOK_URL (optional) | Runs `eas build` in the app repo dir; prints captured logs; updates registry.json last_build_sha; optionally POSTs JSON to Modal webhook |
| `mobile_apps/mobile_app_canary.py` | Iterate registry.json, ping each app's /api/health endpoint in parallel (httpx + ThreadPoolExecutor), and report green/red/missing per app. Deduplicates alerts via .tmp/canary_state.json — only fires on status transitions or consecutive-failure threshold breaches. Designed to be invoked manually or from a Modal cron. | CLI: --dry-run (skip HTTP, just parse registry), --timeout <seconds> (default 10), --alert {none,console,webhook,both} (default console), --webhook-url <url> (or env MOBILE_CANARY_WEBHOOK_URL), --alert-threshold <N> (default 3), --silence-first-run (suppress alerts for checks seen for the very first time); reads execution/mobile_apps/registry.json | Per-app status line + final JSON summary to stdout; .tmp/canary_state.json updated each run; alert banner/webhook on transition or threshold breach; exit 0 if all green or --dry-run, exit 1 if any red |
| `mobile_apps/play_console_tester_gate.py` | Pure date-math reader over registry.json — zero Google API calls. For each app, computes days_remaining toward the Google 20-tester / 14-day gate and reports gate_open status. Supports `--set-started` and `--set-count` to manually update the gate fields from Play Console. | CLI: --app <slug> (optional, defaults to all), --set-started <slug>, --set-count <slug> <n>; reads execution/mobile_apps/registry.json | Human-readable gate status to stdout; mutates registry.json on --set-* commands |
| `mobile_apps/preflight.py` | Mobile-app preflight gate. Runs every CLI / account / env-var check required to ship a mobile app and emits a structured JSON report. Exit 0 iff all required items pass. | env vars (APPLE_ENROLLMENT_STATUS, EXPO_TOKEN, OPENROUTER_API_KEY, FIRECRAWL_API_KEY); host shell with node/eas/wrangler/modal/py on PATH. CLI: --json (machine-readable output), --required-only (skip optional Phase 4-5 keys). | human table on stdout; machine-readable JSON on stdout when --json is set; exit code 0 if all required items GREEN, 1 if any required item is RED. APPLE_ENROLLMENT_STATUS=pending blocks Phase 4-5 but does NOT make exit non-zero (Phases 1-3 are still green). |
| `mobile_apps/testflight_invite.py` | Add emails to TestFlight (internal or external group) via the App Store Connect API using JWT auth (ES256 with the App-Store-Connect private key). Reads ASC_KEY_ID, ASC_ISSUER_ID, ASC_PRIVATE_KEY_PATH from .env. App is resolved from registry.json by slug (uses its `ios_bundle_id`). | CLI: --app <slug>, --emails <email1,email2,...>, --group <internal|external>; env: ASC_KEY_ID, ASC_ISSUER_ID, ASC_PRIVATE_KEY_PATH | HTTPS POSTs to https://api.appstoreconnect.apple.com; prints per-email success/failure |
| `personal_workflows/_archived/job_search_sheet_v1.py` | Main orchestrator for the job-search-sheet pipeline. Runs the full DAG (Stages 0-6): bootstrap + idempotency check, multi-source discovery via Adzuna+Jooble+FranceTravail, keyword filter, LLM relevance gate, dedup with Jaccard guard, sheet materialization with Status/Notes preservation. | Optional CLI flags --mock, --dry-run, --title <name>, --geo <iso2>, --sheet-id <override>, --no-llm. Env vars: SHEETS_SPREADSHEET_ID, GOOGLE_SERVICE_ACCOUNT_PATH, ADZUNA_APP_*, JOOBLE_API_KEY, ANTHROPIC_API_KEY, GEMINI_API_KEY. | Writes the Google Sheet 'Job Applications' (6 visible tabs + _meta). Appends a run summary line to .tmp/job_search/job_search_runs.jsonl. |
| `personal_workflows/_jt_utils.py` | Standalone utility helpers (retry, hashing, normalization, JSON I/O, logging) for the French PM/PO Job Tracker. Imported by other job-tracker modules; not run directly. | None — utility module. | N/A. |
| `personal_workflows/anthropic_watch/fetch.py` | — | — | — |
| `personal_workflows/anthropic_watch/run.py` | — | — | — |
| `personal_workflows/anthropic_watch/tests/front_door.py` | — | — | — |
| `personal_workflows/cv_builder.py` | Generate an ATS-optimized, French-language PDF CV for Debanjan Mazumdar | --company (str), --role (str) via CLI args | .tmp/cv_{company}_debanjan_mazumdar.pdf |
| `personal_workflows/cv_builder_ai_pm_en.py` | Generate the ATS-optimized, English-language, AI PM GENERIC variant of the | --out (path; default .tmp/cv_ai_pm_debanjan_mazumdar_en.pdf) | the PDF at --out (exactly 2 pages) |
| `personal_workflows/cv_builder_ai_pm_fr.py` | Generate the ATS-optimized, French-language, AI PM GENERIC variant of the | --out (path; default .tmp/cv_ai_pm_debanjan_mazumdar_fr.pdf) | the PDF at --out (exactly 2 pages) |
| `personal_workflows/cv_builder_core.py` | Shared reportlab rendering primitives for cv_builder*.py variants. | Imported as a module — not run directly. | N/A (library module) |
| `personal_workflows/cv_builder_dior_itlead_en.py` | Generate the English, ATS-optimized CV for Debanjan Mazumdar targeted at the | --out (path; default .tmp/cv_dior_itlead_debanjan_mazumdar_en.pdf) | the PDF at --out (exactly 2 pages) |
| `personal_workflows/cv_builder_en.py` | Generate an ATS-optimized, English-language PDF CV for Debanjan Mazumdar | --company (str), --role (str) via CLI args | .tmp/cv_{company}_debanjan_mazumdar_en.pdf |
| `personal_workflows/cv_builder_pm_en.py` | Generate the ATS-optimized, English-language, GENERIC Senior-Product-Manager PDF | --out (path; default .tmp/cv_pm_master_debanjan_mazumdar_en.pdf) | the PDF at --out (exactly 2 pages) |
| `personal_workflows/cv_builder_pm_fr.py` | Generate the ATS-optimized, French-language, GENERIC Senior-Product-Manager PDF CV | --out (path; default .tmp/cv_pm_master_debanjan_mazumdar.pdf) | the PDF at --out (exactly 2 pages) |
| `personal_workflows/cv_builder_skott.py` | Generate the French 2-page CV "CV MAZUMDAR Debanjan.pdf" targeted at the | --output (optional path; default "<workspace root>/CV MAZUMDAR Debanjan.pdf") | the PDF at the output path; prints page count sanity hint |
| `personal_workflows/cv_optimizer_local/cli.py` | — | — | — |
| `personal_workflows/cv_optimizer_local/tests/front_door.py` | — | — | — |
| `personal_workflows/cv_optimizer_v2/tests/front_door.py` | — | — | — |
| `personal_workflows/interview_iag/tests/acceptance_agentup.py` | — | — | — |
| `personal_workflows/interview_iag/tests/g_eval_scoring.py` | — | — | — |
| `personal_workflows/interview_iag/tests/live_5_turn_flow.py` | — | — | — |
| `personal_workflows/jd_keyword_research.py` | Collect >=25 FR + >=25 EN Product-Manager job descriptions from FREE reused | — | — |
| `personal_workflows/job_digest/acceptance.py` | Generic acceptance gate for job_digest — a final sanity check run | digest: list[tuple[NormalizedJob, RankedJob]] (the rows about to be | (passed: bool, problems: list[str]) — problems is empty when passed. |
| `personal_workflows/job_digest/cli.py` | CLI entry point for job_digest — `python -m execution.personal_workflows.job_digest.cli {validate|preview|run|render-workflow|setup|doctor}`. | argparse subcommands; each takes a profile.yaml path except `setup`. | stdout (human-readable report per subcommand); `run` also produces |
| `personal_workflows/job_digest/contracts.py` | Typed contracts (Pydantic v2) shared by every layer of job_digest. | imported by sources/, normalizer/, ranker/, notifier/, eval/ | SourceJob, NormalizedJob, RankedJob, JobSource, JobTier — single source of truth. |
| `personal_workflows/job_digest/normalizer/filters.py` | Profile-driven filters for job_digest, implementing exactly the | list[NormalizedJob], profile_schema.Profile | (kept: list[NormalizedJob], stats: dict[str, int]) — stats counts drops |
| `personal_workflows/job_digest/normalizer/normalize.py` | SourceJob -> NormalizedJob mapper for job_digest. Generic (no country | list[SourceJob] | list[NormalizedJob] with content_hash + canonical_url filled in; batch_normalize |
| `personal_workflows/job_digest/normalizer/state.py` | Persistent dedup + email-lock state for job_digest, JSON-file backed | list[NormalizedJob] (filter_new/mark_seen); a state directory path. | — |
| `personal_workflows/job_digest/notifier/email.py` | Build and send the job_digest daily email (friend-facing, per-profile). | — | — |
| `personal_workflows/job_digest/notifier/sheet.py` | Write job_digest results to the friend's own Google Sheet — one | — | — |
| `personal_workflows/job_digest/profile_schema.py` | profile.yaml schema + loader for job_digest. Every user-facing parameter lives here. | path to profile.yaml | validated Profile object; ProfileError with ALL problems listed (never just the first) |
| `personal_workflows/job_digest/ranker/anthropic.py` | Optional Claude Sonnet shortlist reranker for job_digest (rung 3 of | top: list[tuple[NormalizedJob, RankedJob]] (already the top-N by prior | list[RankedJob] | None, same length and order as `top` — None means |
| `personal_workflows/job_digest/ranker/gemini.py` | Optional Gemini 2.5 Flash LLM-judge ranker for job_digest (rung 2 of | list[NormalizedJob] (post-filter), profile_schema.Profile, api_key: str | list[RankedJob] | None — None means "skip this rung" (SDK missing, no |
| `personal_workflows/job_digest/ranker/heuristic.py` | Deterministic (no-API-key) ranker for job_digest, driven entirely by | list[NormalizedJob] (post-filter), profile_schema.Profile | list[RankedJob], 1:1 with the input jobs, ranker_model="heuristic-v1" |
| `personal_workflows/job_digest/ranker/rank.py` | Ranking dispatcher for job_digest — runs the three optional rungs in | list[NormalizedJob] (post-filter), profile_schema.Profile, optional | tuple[list[RankedJob], dict[str, str]] — the ranked jobs (1:1 with the |
| `personal_workflows/job_digest/registry.py` | Static country registry for job_digest — the only place per-country facts live. | none (pure data); imported by profile_schema, sources, filters, templates | COUNTRIES dict keyed by ISO2, plus helper lookups. |
| `personal_workflows/job_digest/run.py` | Orchestrator for job_digest — fetch -> normalize -> dedup -> filter -> | — | — |
| `personal_workflows/job_digest/setup_wizard.py` | Interactive terminal wizard that produces a valid job_digest | — | writes a commented profile.yaml (path from --out, default ./profile.yaml) |
| `personal_workflows/job_digest/sources/france_travail.py` | France Travail "Offres d'emploi v2" official REST API source adapter for | — | — |
| `personal_workflows/job_digest/sources/hellowork.py` | Hellowork source adapter for job_digest, using the public web search plus | — | — |
| `personal_workflows/job_digest/sources/linkedin_guest_api.py` | LinkedIn jobs-guest (public, unauthenticated) API source adapter for job_digest. | — | — |
| `personal_workflows/job_digest/sources/remoteok.py` | RemoteOK source adapter for job_digest. Hits the public unauthenticated | — | — |
| `personal_workflows/job_digest/sources/weworkremotely.py` | WeWorkRemotely source adapter for job_digest. Hits the public RSS feed, | — | — |
| `personal_workflows/job_digest/sources/wttj_algolia.py` | Welcome to the Jungle (WTTJ) source adapter for job_digest, using their | — | — |
| `personal_workflows/job_digest/tests/_helpers.py` | Shared test helpers for job_digest's offline test suite. | none (pure helper functions) | factory functions used by tests/test_*.py to build NormalizedJob / |
| `personal_workflows/job_digest/tests/test_acceptance.py` | Offline tests for acceptance.py's generic sanity gate. | none (synthetic NormalizedJob/RankedJob fixtures) | pytest assertions |
| `personal_workflows/job_digest/tests/test_bundle_smoke.py` | End-to-end smoke test for the packaged, standalone job-digest | none (everything happens under tmp_path / a subprocess) | pytest assertions on subprocess exit codes and stdout content. |
| `personal_workflows/job_digest/tests/test_cli.py` | Offline tests for cli.py's subcommand functions, run.run() replaced with | none (synthetic argparse.Namespace via build_parser().parse_args) | pytest assertions |
| `personal_workflows/job_digest/tests/test_email_build.py` | Offline unit tests for notifier/email.py's build_digest(). | synthetic NormalizedJob/RankedJob pairs and a minimal Profile — no network. | pytest assertions on subject format, HTML-escaping of untrusted job |
| `personal_workflows/job_digest/tests/test_filters.py` | Offline tests for normalizer/filters.py. | none (synthetic NormalizedJob fixtures via tests/_helpers.py) | pytest assertions |
| `personal_workflows/job_digest/tests/test_heuristic.py` | Offline tests for ranker/heuristic.py — combine() weighting/hard-zero | none (synthetic NormalizedJob fixtures via tests/_helpers.py) | pytest assertions |
| `personal_workflows/job_digest/tests/test_normalize.py` | Offline tests for normalizer/normalize.py's _detect_remote (C3). | none (uses the exact location strings shipped in tests/fixtures*) | pytest assertions |
| `personal_workflows/job_digest/tests/test_package.py` | Tests for scripts/package_job_digest.py — the standalone-bundle builder. | none (builds the zip into a tmp dist dir; never touches the real dist/) | pytest results |
| `personal_workflows/job_digest/tests/test_rank.py` | Offline tests for ranker/rank.py's dispatcher — signature (M4: | none (synthetic NormalizedJob fixtures via tests/_helpers.py) | pytest assertions |
| `personal_workflows/job_digest/tests/test_run_dry.py` | Offline end-to-end test of run.run() in dry mode. Monkeypatches | tmp_path fixtures, monkeypatch | pytest assertions |
| `personal_workflows/job_digest/tests/test_setup_wizard.py` | Offline unit tests for setup_wizard.py's non-interactive | a synthetic answers.json (profile.yaml-shaped) written to tmp_path. | pytest assertions that profile.yaml is written and passes |
| `personal_workflows/job_digest/tests/test_sheet.py` | Offline tests for notifier/sheet.py's pure helpers (no real | none (synthetic NormalizedJob/RankedJob fixtures + fake gspread modules) | pytest assertions |
| `personal_workflows/job_digest/tests/test_sources_dry.py` | Offline pytest coverage for job_digest source adapters. Verifies every | none (pure offline; fixtures under execution/personal_workflows/job_digest/fixtures/, | pytest results |
| `personal_workflows/job_digest/tests/test_state.py` | Offline tests for normalizer/state.py (seen.json dedup + email lock). | tmp_path fixtures only — no network, no real files outside pytest's tmp dir. | pytest assertions |
| `personal_workflows/job_digest/tests/test_workflow_render.py` | Offline unit tests for workflow_render.py's render_workflow() and | synthetic Profile objects (Europe/Paris, America/New_York — northern | pytest assertions that the rendered YAML parses and carries the |
| `personal_workflows/job_digest/tests/verify_geo_ids.py` | Manual, network-touching geoId verification for job_digest's LinkedIn | — | — |
| `personal_workflows/job_digest/workflow_render.py` | Render the friend's `.github/workflows/job_digest.yml` from their | profile: Profile (digest.hour_local, digest.timezone drive the cron; | render_workflow(profile, *, minute=13, layout="standalone") -> str — |
| `personal_workflows/job_digest_renderer.py` | Build a self-contained HTML digest of active PM/PO jobs (France) first-seen within a configurable window, grouped by company with contacts. | --db (SQLite path), --window (days, default 7), --mark-expired (flag), --out (output path), --print (stdout flag); env var JOB_TRACKER_DB_PATH. | HTML string returned from render_digest_html(); optionally written to --out file or printed to stdout. |
| `personal_workflows/job_search_llm_gate.py` | LLM relevance gate + fit-scorer for the job-search-sheet pipeline. Scores each candidate job (relevance, contract_type, fit_score 0-100, match_identity) in BATCHES (one call per ~10 jobs) to stay within free quota. FREE by default: primary gemini-2.5-flash, secondary gemini-2.5-flash-lite; PAID Anthropic tier only if anthropic_optin=True. Hard cap: 200 jobs per run. | Candidate jobs (list[dict] in RawJob+ schema). Env vars GEMINI_API_KEY (required), ANTHROPIC_API_KEY (only if anthropic_optin). | For each input job: {"relevance": ..., "contract_type": str|None, "fit_score": int, "match_identity": str|None, "reason": str}. |
| `personal_workflows/job_search_notify.py` | Send a plain-text Gmail summary after each job-search-sheet run. | — | — |
| `personal_workflows/job_search_setup.py` | Validates Phase 0 provisioning for job-search-sheet pipeline. Checks env vars, | CLI flags --check-only, --bootstrap, --verbose. Env vars (SHEETS_SPREADSHEET_ID, | Stdout report of [OK] / [--MISSING] per check; exit code 0 if all OK, 1 otherwise. |
| `personal_workflows/job_search_v2/contracts.py` | Typed contracts (Pydantic v2) shared by every layer of job_search_v2. | imported by sources/, normalizer/, ranker/, notifier/, eval/ | SourceJob, NormalizedJob, RankedJob, JobSource, JobTier — single source of truth. |
| `personal_workflows/job_search_v2/normalizer/contract_filter.py` | Contract-type filter for NormalizedJobs. Runs AFTER title_filter and | — | — |
| `personal_workflows/job_search_v2/normalizer/dedup.py` | Persistent cross-day dedup for job_search_v2. SQLite-backed seen-set with TTL. | list[NormalizedJob] from the normalize step | — |
| `personal_workflows/job_search_v2/normalizer/language_filter.py` | Language filter for NormalizedJobs. Drops jobs whose title + | — | — |
| `personal_workflows/job_search_v2/normalizer/location_filter.py` | Location filter for NormalizedJobs. Reads accept + reject patterns from | — | — |
| `personal_workflows/job_search_v2/normalizer/normalize.py` | SourceJob -> NormalizedJob mapper. One pure function per source. | list[SourceJob] | list[NormalizedJob] with content_hash + canonical_url filled in. |
| `personal_workflows/job_search_v2/normalizer/title_filter.py` | Title filter for NormalizedJobs. Drops jobs whose title indicates | — | — |
| `personal_workflows/job_search_v2/notifier/email.py` | Send the v2 daily digest. Plain-text email via Gmail SMTP, with a | — | — |
| `personal_workflows/job_search_v2/notifier/health_score.py` | Health Score calculator for job_search_v2. Produces a single | — | — |
| `personal_workflows/job_search_v2/notifier/sheet.py` | Route v2 NormalizedJobs into per-role destination tabs (PM, AI PM, AI Automation, | — | — |
| `personal_workflows/job_search_v2/profile/extract_profile.py` | Extract a structured operator profile from real artifacts (CV PDF + | — | — |
| `personal_workflows/job_search_v2/profile/generate_eval_set.py` | Generate a synthetic labeled gold-set for evaluating the | — | — |
| `personal_workflows/job_search_v2/profile/loader.py` | Single source of truth for profile.json. All screens (title, | — | — |
| `personal_workflows/job_search_v2/purge_irrelevant_rows.py` | Sheet hygiene — removes rows from each role tab + Top Matches | — | — |
| `personal_workflows/job_search_v2/ranker/score.py` | Gemini 2.5 Flash LLM-judge ranker for NormalizedJobs. | — | — |
| `personal_workflows/job_search_v2/ranker/sonnet_rerank.py` | Second-pass shortlist re-ranker using Claude Sonnet 5 via the | — | — |
| `personal_workflows/job_search_v2/run.py` | Orchestrator for job_search_v2. Calls the 4 sources in parallel, normalizes, | — | — |
| `personal_workflows/job_search_v2/sources/_playwright_helpers.py` | Small Playwright helpers shared by WTTJ + APEC + future SPA-gated sources. | imported by source modules | a `render_page()` function that handles the consent + hydration dance. |
| `personal_workflows/job_search_v2/sources/apec.py` | APEC (apec.fr) source adapter — France's executive (cadres) job board. | — | — |
| `personal_workflows/job_search_v2/sources/france_travail.py` | France Travail "Offres d'emploi v2" official REST API source adapter. | — | — |
| `personal_workflows/job_search_v2/sources/hellowork.py` | Hellowork source adapter using the public web search + per-job | — | — |
| `personal_workflows/job_search_v2/sources/hellowork_gmail.py` | Hellowork Job-Alert email ingestion source. Reads Hellowork alert emails | — | — |
| `personal_workflows/job_search_v2/sources/indeed_gmail.py` | Indeed Job-Alert email ingestion source. Reads Indeed alert emails from | — | — |
| `personal_workflows/job_search_v2/sources/jobgether_gmail.py` | Jobgether Job-Alert email ingestion source. Reads Jobgether alert emails | — | — |
| `personal_workflows/job_search_v2/sources/linkedin_gmail.py` | LinkedIn Job-Alert email ingestion source. Reads LinkedIn "Job Alert" emails | — | — |
| `personal_workflows/job_search_v2/sources/linkedin_guest_api.py` | LinkedIn jobs-guest (public unauthenticated) API source adapter. Hits the | — | — |
| `personal_workflows/job_search_v2/sources/remoteok.py` | RemoteOK source adapter. Hits the public unauthenticated JSON | — | — |
| `personal_workflows/job_search_v2/sources/weworkremotely.py` | WeWorkRemotely source adapter. Hits the public RSS feeds for the | — | — |
| `personal_workflows/job_search_v2/sources/wttj.py` | Welcome to the Jungle (WTTJ) source adapter — parses the public job-list | — | — |
| `personal_workflows/job_search_v2/sources/wttj_algolia.py` | Welcome to the Jungle (WTTJ) source adapter using their public Algolia | — | — |
| `personal_workflows/job_search_v2/trim_sheet_columns.py` | One-shot migration that rewrites every role tab + Top Matches tab | — | — |
| `personal_workflows/job_tracker_db.py` | SQLite persistence layer for the French PM/PO Job Tracker. Manages companies, jobs, contacts, and notification logs with upsert semantics and time-windowed queries. | db_path (Path or str) passed to init_db(); structured dicts passed to each upsert function. | sqlite3.Connection (from init_db); row ids and boolean flags from upsert functions; list[dict] from query functions. |
| `personal_workflows/job_tracker_pm_france.py` | Main daily orchestrator for the French PM/PO Job Tracker. Discovers jobs across | CLI flags (--boards, --mock, --dry-run, --no-enrich, --no-resolve, --send, --db, | SQLite DB rows (companies, jobs, contacts, notifications_log); .tmp/job_tracker/<run_id>/ |
| `personal_workflows/job_tracker_setup.py` | One-shot setup helper for the French PM/PO Job Tracker. Validates required | CLI flags (--check-only, --install-deps, --init-db); env vars from .env. | Printed status report (stdout); optional DB file created at resolved path; |
| `personal_workflows/personal_brand/check_metric_coherence.py` | Hard-fail gate asserting CV (EN/FR PDFs), LinkedIn copy, and the | none (reads sibling files + portfolio_site content + personal_brand/cv PDFs) | stdout report; exit 0 = coherent, exit 1 = contradiction found |
| `personal_workflows/personal_brand/upwork_portfolio_generator/render.py` | — | — | — |
| `personal_workflows/portfolio_site/scripts/fetch_malt_recs.py` | — | — | — |
| `personal_workflows/portfolio_site/scripts/make_linkedin_banner.py` | — | — | — |
| `personal_workflows/portfolio_site/scripts/wire_domain.py` | — | — | — |
| `personal_workflows/prodcraft_audio_postprocess.py` | Post-process an existing TTS WAV to add introspection pauses + adjust speed, WITHOUT calling F5-TTS again. Uses the Whisper segments JSON to find sentence boundaries (segments ending in . ! ?) and inserts silence at those points. Then applies a single ffmpeg atempo for the final speed. | — | — |
| `personal_workflows/prodcraft_beats.py` | Plan visual beats for a ProdCraft video. Feeds Whisper segments (text + start/end times) to Gemini 2.5 Flash, gets back a sequence of beats covering all segments, each classified as stock | diagram | text_card with a payload (search query, FLUX prompt, or card text). Resolves segment indices back to seconds for the Remotion composition. | — | — |
| `personal_workflows/prodcraft_chatterbox_modal.py` | Modal app deploying ChatterboxTTS (Resemble AI, MIT) as an alternative to F5-TTS. ChatterboxTTS handles long-form conversational/educational content more reliably than F5 — no reference bleeding, no word scrambling at paragraph scale. Same `.synthesize(ref_audio_bytes, ref_text, gen_text)` interface as the F5 wrapper for drop-in swap. | — | — |
| `personal_workflows/prodcraft_dashboard.py` | Streamlit dashboard for the ProdCraft autopilot approval gate. Lists pending videos, plays each MP4 inline, exposes title/description/tags editors, and Approve/Reject buttons. Backed by execution/personal_workflows/prodcraft_queue.py state. | — | — |
| `personal_workflows/prodcraft_f5_modal.py` | Modal app deploying F5-TTS on a GPU container so we are no longer rate-limited by HuggingFace ZeroGPU. Single class with a `synthesize` method that takes ref audio bytes + ref text + gen text and returns rendered wav bytes. Volume-mounts the HF cache so model weights persist across cold starts. Deploy once with `modal deploy execution/personal_workflows/prodcraft_f5_modal.py`; then call from the workspace via `F5TTSModal.synthesize.remote(...)`. | — | — |
| `personal_workflows/prodcraft_medspa/audit/audit_site.py` | Orchestrate the per-business website audit (fetch, signals, PSI, screenshots, vision, score). | --metro X [--business-id ID] [--limit N] [--mock] [--store ...] [--store-root PATH] | One `audits` row per audited business (with `raw`), an `events` log entry, and a stdout stat line. |
| `personal_workflows/prodcraft_medspa/audit/booking_detect.py` | Detect online-booking vendor widgets by grepping HTML, script srcs, iframe srcs, and hrefs. | Imported by audit_site.py; no CLI args (importable module). | Pure functions over an HTML string — no filesystem or network side effects. |
| `personal_workflows/prodcraft_medspa/audit/fetch.py` | Fetch a business website's HTML, following redirects and recording SSL validity. | Imported by audit_site.py; no CLI args (importable module). | FetchResult dataclass — no filesystem writes (caller persists via Store). |
| `personal_workflows/prodcraft_medspa/audit/fixtures/vision_golden/make_golden.py` | Generate 6 synthetic PNGs (3 modern, 3 dated) as a golden set for the vision_audit prompt. | None (no CLI args); requires Pillow. | {modern,dated}_{1,2,3}.png in this directory + golden.json with expected score bands. |
| `personal_workflows/prodcraft_medspa/audit/html_signals.py` | Extract viewport/footer-year/analytics/visible-text/title signals from fetched HTML. | Imported by audit_site.py; no CLI args (importable module). | Pure functions over an HTML string — no filesystem or network side effects. |
| `personal_workflows/prodcraft_medspa/audit/psi.py` | PageSpeed Insights v5 wrapper — mobile/desktop performance score + mobile-friendliness. | Imported by audit_site.py; optional PAGESPEED_API_KEY (keyless works at low volume, per PROJECT_SPEC.md). | PsiResult dataclass — no filesystem writes (caller persists via Store). |
| `personal_workflows/prodcraft_medspa/audit/sample_audit.py` | Audit a random seeded sample of businesses in a metro; record a metro_stats row with a Wilson CI. | --metro X --n 40 [--mock] [--seed 42] [--store ...] [--store-root PATH] [--skip-vision] [--skip-screenshots] | One `audits` row per sampled business (skipped for businesses that already have a current audit when |
| `personal_workflows/prodcraft_medspa/audit/scoring.py` | Pure scoring function over stored audit signals. No I/O — CONTRACTS.md "Scoring" table exactly. | Imported by audit_site.py (fresh signals) and scoring.py's own --recompute CLI (stored signals). | score(signals) -> {"total", "bucket", "gaps", "score_version"}; CLI recomputes and diffs stored audits. |
| `personal_workflows/prodcraft_medspa/audit/screenshots.py` | Playwright mobile/desktop screenshots + above-fold CTA detection; upload to store-backed location. | Imported by audit_site.py; optional PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers. | PNG files under .tmp/prodcraft_medspa/screenshots/ and (Supabase store) an uploaded object URL. |
| `personal_workflows/prodcraft_medspa/audit/stats.py` | Wilson score interval helper for the metro-sample qualify-rate confidence interval. | Imported by sample_audit.py; no CLI args (importable module). | Pure function — no filesystem or network side effects. |
| `personal_workflows/prodcraft_medspa/audit/tech_detect.py` | Local (no Apify) fingerprint detection of site builder, WordPress theme/year, and jQuery version. | Imported by audit_site.py; optional network fetch of a WordPress theme's style.css header. | TechResult dataclass — no filesystem writes (caller persists via Store). |
| `personal_workflows/prodcraft_medspa/audit/vision.py` | Claude vision pass over the mobile/desktop screenshots -> dated_score + rationale + signals. | Imported by audit_site.py; prompts/vision_audit.md rendered with business_name/final_url. | dict of vision_dated_score/vision_rationale/vision_model_id/vision_prompt_sha256 (+ raw signals). |
| `personal_workflows/prodcraft_medspa/common/config.py` | Repo-root bootstrap, .env loading, and typed Settings for the ProdCraft med-spa pipeline. | env vars listed in CONTRACTS.md ("Secrets only via env"); no CLI args (imported, not run). | Settings dataclass instance from bootstrap(); .tmp/prodcraft_medspa/ created on demand. |
| `personal_workflows/prodcraft_medspa/common/http.py` | Shared requests.Session with retry/backoff and a fixed timeout, plus a stable User-Agent. | Imported by discovery/, audit/, enrich/, preview/ scripts; no CLI args (importable module). | A configured requests.Session and get()/post() wrappers. |
| `personal_workflows/prodcraft_medspa/common/llm.py` | Thin wrapper over the Anthropic SDK for prompt-file-driven calls (extraction, vision scoring). | Imported by audit/, enrich/, outreach/ scripts; no CLI args (importable module). | {"text", "model_id", "prompt_sha256", "usage": {4 token counts}, "cost_usd"} envelope dict. |
| `personal_workflows/prodcraft_medspa/common/models.py` | Plain dataclasses mirroring db/schema.sql tables, with to_row()/from_row() round-trips. | dict rows from Store implementations (JSON for LocalStore, PostgREST JSON for SupabaseStore). | Dataclass instances; to_row() dicts ready for Store upsert/insert calls. |
| `personal_workflows/prodcraft_medspa/common/notify.py` | Telegram error-channel notifier. Every script's top-level failure calls error(). | --sample (CLI); env TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID, PRODCRAFT_ENV. | A Telegram sendMessage call; stderr log line and False return when env is unset (no-op, no crash). |
| `personal_workflows/prodcraft_medspa/common/slug.py` | Slugify business names and build unguessable preview-subdomain hosts. | Imported by preview/ and discovery/ scripts; no CLI args (importable module). | Pure functions — no filesystem or network side effects. |
| `personal_workflows/prodcraft_medspa/common/store.py` | Store Protocol + LocalStore (JSON files) and SupabaseStore (PostgREST) implementations. | Imported by every stage script; env PRODCRAFT_STORE, SUPABASE_URL, SUPABASE_SERVICE_KEY. | LocalStore writes one JSON file per table under its root dir; SupabaseStore makes HTTPS calls. |
| `personal_workflows/prodcraft_medspa/db/apply_schema.py` | Apply schema.sql (Supabase/Postgres) then seed chains + config into the store. | --store {local,supabase} (default env PRODCRAFT_STORE, fallback local); --root (LocalStore dir); | Schema applied to Postgres (or printed for manual paste); seed_chains.json/seed_config.json |
| `personal_workflows/prodcraft_medspa/deals/deals.py` | Deal recording, go-live, and the 60-day booking-rate guarantee proof (PROJECT_SPEC.md | CLI subcommands: | stdout JSON stat lines per subcommand; Store mutations via store.upsert_deal and, on |
| `personal_workflows/prodcraft_medspa/discovery/places_search.py` | Tile a metro's target suburbs, run Google Places Text Search (New) per tile/query, dedupe, | --metro (required), --mock, --store {local,supabase}, --store-root PATH, --queries "a,b,c", | Upserted `businesses` rows (every found unique place, dropped or kept, carries a drop_reason); |
| `personal_workflows/prodcraft_medspa/discovery/tiles.py` | Tile dataclass + loader for per-metro Google Places search tiles (suburb-centered circles). | tiles/{metro}.json files (metro, state, tiles[]); imported by places_search.py and tests. | Tile dataclasses; pure functions, no filesystem writes, no network. |
| `personal_workflows/prodcraft_medspa/discovery/verify_counts.py` | Print a per-suburb business count table from the store, optionally compared against a manual | --metro (required), --store {local,supabase}, --store-root PATH, --manual-counts PATH | A printed table to stdout; one JSON stat line: {"script","metro","suburbs","flagged"}. |
| `personal_workflows/prodcraft_medspa/enrich/apollo.py` | Waterfall step 4 — Apollo.io `people/match` by name+domain (when StepContext.owner_name is | business dict, enrich.context.StepContext (uses ctx.domain, ctx.owner_name); env APOLLO_API_KEY. | enrich.context.StepResult; on a hit, email_source="apollo". |
| `personal_workflows/prodcraft_medspa/enrich/contact_page.py` | Waterfall step 1 — fetch the site home page plus /about, /about-us, /contact, /team, | business dict (website_url/final_url), enrich.context.StepContext. | enrich.context.StepResult; on a hit, email_source="contact_page". |
| `personal_workflows/prodcraft_medspa/enrich/context.py` | Shared StepContext/StepResult dataclasses threaded through every enrichment waterfall step. | N/A (imported by waterfall.py and each step module — not runnable on its own). | dataclass definitions only. |
| `personal_workflows/prodcraft_medspa/enrich/findymail.py` | Waterfall step 5a — Findymail `search/name` (name + domain) for an email address. | business dict, enrich.context.StepContext (uses ctx.domain, ctx.owner_name); env FINDYMAIL_API_KEY. | enrich.context.StepResult; on a hit, email_source="findymail". |
| `personal_workflows/prodcraft_medspa/enrich/fixtures/seed_mock_store.py` | Seeds a LocalStore with 10 "qualified" businesses (+2 helper rows used only by isolated | a Store instance (LocalStore in tests; importable, not runnable on its own). | businesses + audits rows written into the given store; returns the seeded business dicts. |
| `personal_workflows/prodcraft_medspa/enrich/gbp_reviews.py` | Waterfall step 2 — Google Places (New) `GET places/{id}` with field mask `reviews` (5 max). | business dict (place_id), enrich.context.StepContext. | enrich.context.StepResult; on a hit, email_source="gbp_reviews". |
| `personal_workflows/prodcraft_medspa/enrich/generic_inbox.py` | Waterfall step 6 — final fallback. Uses a generic mailbox already seen on the site | business dict, enrich.context.StepContext (uses ctx.domain, ctx.generic_email). | enrich.context.StepResult; on a hit, email_source="generic_inbox". |
| `personal_workflows/prodcraft_medspa/enrich/hunter.py` | Waterfall step 5b — Hunter `email-finder` (name + domain), tried after Findymail per | business dict, enrich.context.StepContext (uses ctx.domain, ctx.owner_name); env HUNTER_API_KEY. | enrich.context.StepResult; on a hit, email_source="hunter". |
| `personal_workflows/prodcraft_medspa/enrich/names.py` | Shared regex utilities for owner-name and mailbox extraction, used by contact_page.py, | free text (page HTML/text, review text, registry HTML). | list[str] name candidates; (personal[], generic[]) email address lists. |
| `personal_workflows/prodcraft_medspa/enrich/state_registry.py` | Waterfall step 3 — best-effort state business-registry lookups (IL SOS, MN SOS, OH SOS, | business dict (name, state), enrich.context.StepContext. | enrich.context.StepResult (hit is always False); `lookup(name, state, ctx=...)` is the |
| `personal_workflows/prodcraft_medspa/enrich/verify.py` | Waterfall step 7 — verify the chosen email via MillionVerifier. Wraps | email str; mock: bool; fixtures_root: Path; api_key: str | None. | (email_status, cost_usd) tuple — email_status in {deliverable, risky, undeliverable, unknown}. |
| `personal_workflows/prodcraft_medspa/enrich/waterfall.py` | Enrichment waterfall CLI. For every business in --metro whose latest audit bucket is | --metro (required), --business-id, --min-score (default 45), --mock, --store {local,supabase}, | businesses rows updated in the store; one stat-line JSON on stdout: |
| `personal_workflows/prodcraft_medspa/outreach/_store_helpers.py` | Thin, business-shaped wrappers over the Store Protocol's generic list_rows() for the | Imported by outreach/* modules; no CLI args (importable module). | Plain lists of dicts; no filesystem/network side effects beyond the underlying Store call. |
| `personal_workflows/prodcraft_medspa/outreach/advance.py` | Follow-up scheduler. For `sent` rows whose next_touch_at <= date, touch < 4, and | CLI: [--date YYYY-MM-DD] [--mock] [--store {local,supabase}] [--store-root PATH]. | stdout stat line {"script":"advance","date":...,"advanced":n,"closed_lost":n}; |
| `personal_workflows/prodcraft_medspa/outreach/daily_queue.py` | Build and print today's manual-send outreach queue — enqueue new touch-1 rows, | CLI: [--date YYYY-MM-DD] [--mock] [--store {local,supabase}] [--store-root PATH] | stdout: the queue table, then a JSON stat line: |
| `personal_workflows/prodcraft_medspa/outreach/draft_email.py` | Render one outreach draft (subject + body) from a human-written template pool | Imported by daily_queue.py: render_draft(store, settings, outreach_row=..., business=..., | {"subject": str, "body": str, "variant": "a"|"b"|"c"|None, "llm": envelope|None}. |
| `personal_workflows/prodcraft_medspa/outreach/fixtures/seed_mock_store.py` | Seed a LocalStore with businesses/audits/previews/config that exercise | seed(store) -> None (importable, used by tests/prodcraft_medspa/test_outreach.py and by | Populates the given LocalStore's businesses/audits/previews/config tables. |
| `personal_workflows/prodcraft_medspa/outreach/gmail_drafts.py` | Create a Gmail draft (never sends) via the Gmail API users.drafts.create, following | create_draft(to_email, subject, body, sender_name, settings, mock, business_id=None); | live -> one Gmail draft via users.drafts.create; --mock -> a .eml file under |
| `personal_workflows/prodcraft_medspa/outreach/gmail_reader.py` | Read inbound Gmail replies for scan_replies.py. Live path uses the Gmail API | Imported by scan_replies.py. Live: env GMAIL_TOKEN_JSON (see gmail_drafts.py for the | list[dict] reply records: {"thread_id", "from", "to", "subject", "body_text", |
| `personal_workflows/prodcraft_medspa/outreach/lint_draft.py` | CAN-SPAM linter implementing prompts/can_spam_checklist.md exactly, fail-closed. | Imported by daily_queue.py (lint(subject, body, touch=..., sender_name=..., sender_physical_address=...)); | {"violations": [...], "needs_operator_input": bool, "word_count": int}; stdout JSON via CLI. |
| `personal_workflows/prodcraft_medspa/outreach/scan_replies.py` | Poll Gmail for replies to `sent` outreach rows, classify each with | CLI: [--mock] [--store {local,supabase}] [--store-root PATH] [--since-days 14]. | Store mutations via state_machine.transition / store.update_outreach / |
| `personal_workflows/prodcraft_medspa/outreach/state_machine.py` | Explicit outreach status state machine (CONTRACTS.md "Outreach state machine" section, | Imported by daily_queue.py/scan_replies.py/advance.py; also runnable directly for --help. | transition() mutates the Store (update_outreach / upsert_outreach / upsert_business / |
| `personal_workflows/prodcraft_medspa/outreach/transition.py` | CLI for outreach/state_machine.py's transition() — the Python-side state-machine | CLI: --outreach-id ID --to STATE [--sent-at ISO] [--sentiment ...] [--notes ...] | stdout JSON of the resulting (or newly-created, for sent->queued) outreach row plus a |
| `personal_workflows/prodcraft_medspa/preview/approve.py` | CLI counterpart of the dashboard's Previews tab approve button. Moves `previews` rows from | --preview-id ID (one preview) | --metro X --all-review (every review-status, non-takedown preview | `previews.status` = approved, `events` rows (entity=preview, event=status:review->approved), |
| `personal_workflows/prodcraft_medspa/preview/build_preview.py` | Assembles business.json per CONTRACTS.md for each qualified/deliverable/contactable business, | --metro X | --business-id ID (one required), --limit N, --mock, --store {local,supabase}, | template/business.json written + `npm run build` run (unless --skip-build/synthesized), files |
| `personal_workflows/prodcraft_medspa/preview/content_lint.py` | Content-policy linter over an assembled business.json — forbidden medical/legal terms, any URL | Imported by build_preview.py as lint(business_json) -> list[str]. Standalone CLI: --file business.json. | Pure function, no filesystem/network side effects (aside from the optional CLI's own file read). |
| `personal_workflows/prodcraft_medspa/preview/extract_services.py` | Build business.json's services[] + tagline via common.llm (prompts/extract_services.md), | Imported by build_preview.py: extract_services(business, audit, *, mock, model, fixtures_root) -> dict. | {"services": [...], "tagline": str, "model_id", "prompt_sha256", "llm_cost_usd"}. |
| `personal_workflows/prodcraft_medspa/preview/publish.py` | Talks to the preview/worker Cloudflare Worker's control-plane API — POST /api/publish, | Imported by build_preview.py/takedown.py: publish()/extend()/get_meta()/takedown(). Standalone CLI: | HTTPS POST/GET to the Worker (live) or JSON files under .tmp/prodcraft_medspa/kv/{host}.json (mock). |
| `personal_workflows/prodcraft_medspa/preview/r2.py` | S3-compatible SigV4 client for Cloudflare R2 (stdlib hmac/hashlib over requests, no boto3), | Imported by build_preview.py/takedown.py. Env R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, | R2Client.upload_dir/delete_prefix make HTTPS calls to R2; MockR2 writes/deletes under a local dir. |
| `personal_workflows/prodcraft_medspa/preview/takedown.py` | Programmatic takedown of one or more previews (by --business-id, --preview-id, or --host). | --business-id ID | --preview-id ID | --host HOST (exactly one), --mock, --store {local,supabase}, | R2 objects deleted, a POST to the Worker's /remove, `previews`/`businesses`/`outreach` rows updated, |
| `personal_workflows/prodcraft_medspa/scripts/_stage_runner.py` | Shared subprocess-invocation helpers for run_metro.py and daily.py. Every other | Imported only; no CLI args, no env vars of its own (resolve_store_kind reads | dict results describing each subprocess invocation; no files written. |
| `personal_workflows/prodcraft_medspa/scripts/daily.py` | The operator's morning command. Runs outreach.advance -> outreach.scan_replies -> | CLI: [--date D] [--mock] [--store {local,supabase}] [--store-root P] [--replies-only] | stdout: per-stage results + the daily queue (or the phase0 gate state with |
| `personal_workflows/prodcraft_medspa/scripts/doctor.py` | For every pipeline stage, lists each required env var present/missing (never prints | CLI: [--live] [--stages discovery,audit,enrich,preview,outreach,store,notify]. Env: every | stdout: env-presence table, and with --live, the live-check table + Node/Chromium/ |
| `personal_workflows/prodcraft_medspa/scripts/fit_weights.py` | Reads audits + outreach from the store, computes per-signal reply rate (touch-1 | CLI: [--mock] [--store {local,supabase}] [--store-root P] [--min-sent 50]. Env: store vars. | stdout table + JSON stat line; .tmp/prodcraft_medspa/fit_weights.json (per-signal reply |
| `personal_workflows/prodcraft_medspa/scripts/run_metro.py` | Orchestrator that chains discovery -> audit -> enrich -> preview for one metro, each | CLI: --metro X [--mock] [--store {local,supabase}] [--store-root P] | stdout funnel table + JSON summary; .tmp/prodcraft_medspa/runs/{metro}_{timestamp}.json run |
| `personal_workflows/prodcraft_medspa/scripts/sync_sheets.py` | Mirrors the read-only `v_pipeline` view (db/schema.sql) to a Google Sheet tab named | CLI: [--mock] [--store {local,supabase}] [--store-root P]. Env: GOOGLE_SHEETS_MIRROR_ID, | --mock: .tmp/prodcraft_medspa/v_pipeline.csv. Live: overwrites the "pipeline" tab of the |
| `personal_workflows/prodcraft_medspa/template/node_modules/flatted/python/flatted.py` | — | — | — |
| `personal_workflows/prodcraft_oauth.py` | Shared OAuth flow for the ProdCraft autopilot. Owns the bootstrap + refresh of the YouTube refresh token; provides `get_credentials()` to other scripts (ingest, upload). | — | — |
| `personal_workflows/prodcraft_orchestrate.py` | End-to-end ProdCraft autopilot orchestrator. Takes a topic, runs script_gen → voice_clone (gemini) → transcribe → living_prd_plan_gen → render_living_prd → thumbnail → queue add. Outputs a ready-for-review video in the pending queue. | — | — |
| `personal_workflows/prodcraft_queue.py` | Manage the ProdCraft autopilot pending-approval queue. Add a rendered video + metadata to the queue, list pending items, approve (move to approved/), reject (move to rejected/). | — | — |
| `personal_workflows/prodcraft_schedule.py` | Scheduler entrypoint. Picks the next unprocessed topic from config/prodcraft_topics.yml, runs prodcraft_orchestrate.py end-to-end, then marks the topic processed in the YAML. Designed to be called by a weekly cron (GitHub Actions or local Task Scheduler). | — | — |
| `personal_workflows/prodcraft_script_gen.py` | Generate a ProdCraft YouTube script via Gemini 2.5 Flash (default), GLM 5.2 (personal mode), or Sonnet 4.6 (client mode), conditioned on the distilled voice profile. | — | — |
| `personal_workflows/prodcraft_transcribe.py` | Word-level transcription using faster-whisper. Produces a JSON of (word, start_sec, end_sec) tuples that drives visual-beat timing for ProdCraft Phase 1. | — | — |
| `personal_workflows/prodcraft_tts.py` | TTS wrapper for the ProdCraft pipeline. OpenAI tts-1 by default; voice + model configurable. Tracks monthly char-count to KV file (per plan §Cost circuit-breakers — 220k char/mo hard-stop). | — | — |
| `personal_workflows/prodcraft_visuals.py` | Fetch visuals for each visual_beats.json beat. stock → Pexels API photo; diagram → FLUX.1-schnell on HuggingFace free tier; text_card → skipped (Remotion renders text natively). Re-runnable: skips beats whose asset already exists on disk. | — | — |
| `personal_workflows/prodcraft_voice_clone.py` | F5-TTS voice clone via the public mrfakename/E2-F5-TTS HuggingFace Space. Free path (no Replicate billing); rate-limited by the Space's queue. Uses .tmp/prodcraft/voice_sample/ as the reference (created by prodcraft_voice_sample.py). | — | — |
| `personal_workflows/prodcraft_voice_profile.py` | Distill ingested ProdCraft transcripts into a SCRIPT-WRITING voice profile (different shape from execution/modules/creator_profiles.py which is for analyzer use). One Gemini 2.5 Flash call, free tier. | — | — |
| `personal_workflows/prodcraft_voice_sample.py` | Extract a clean 6-30s voice sample + matching transcript from a ProdCraft YouTube video for F5-TTS voice cloning. Downloads audio + auto-vtt via yt-dlp, picks a continuous-speech window from the middle of the video (skips intro/outro), trims with ffmpeg to 24kHz mono WAV. | — | — |
| `personal_workflows/prodcraft_youtube_upload.py` | Upload an approved ProdCraft video to YouTube via Data API v3 (resumable upload). Reads the oldest approved item from .tmp/prodcraft/queue/approved/, uploads it, then marks the metadata.json with youtube_video_id + uploaded_at and moves the directory to .tmp/prodcraft/queue/uploaded/. | — | — |
| `personal_workflows/self_outbound_system/_common.py` | Shared helpers for the self_outbound_system pipeline. Path helpers, timestamped filename builders, EUR conversion constant, argparse defaults. No side effects at import time. | None (imported by sibling scripts). | Constants and small pure functions. |
| `personal_workflows/self_outbound_system/acceptance.py` | Output-acceptance gate. Runs TWO independent checks that both MUST pass: (a) the frozen regression corpus at tests/acceptance_corpus.json (20 bad must reject, 12 good must pass), and (b) daily Instantly stats (sends>0, bounce<5%, unsub<0.3%, complaints==0). Hard-fails with non-zero exit + raise on any breach. Dry-run skips (b) — corpus check only. | --corpus <path>, --stats <path>, --dry-run/--live, --output <path>. | .tmp/self_outbound/acceptance_<date>.json with per-case results. Exit 0 on PASS, non-zero + SystemExit on FAIL. |
| `personal_workflows/self_outbound_system/canary.py` | Front-door synthetic. Sends 1 real email from the warmed inbox to a monitored Gmail address, then IMAP-polls to confirm INBOX placement (not SPAM / CATEGORY_PROMOTIONS). Hard-fails the daily run on placement failure. Dry-run returns a canned PASS without sending or polling. | --dry-run/--live, --send-test-email (live only), --output <path>. Env (live only): INSTANTLY_INBOX_EMAIL, CANARY_DESTINATION_EMAIL, CANARY_IMAP_HOST, CANARY_IMAP_USER, CANARY_IMAP_APP_PASSWORD. | .tmp/self_outbound/canary_<date>.json with {status, placement, latency_s, ...}. Exit 0 on PASS, exit 1 on FAIL. |
| `personal_workflows/self_outbound_system/digest.py` | Build the daily or weekly digest message from today's .tmp/self_outbound/*.json artifacts. Dry-run prints to stdout; live mode (STUB) would send to Telegram + CC debolshop@gmail.com. Includes anomaly flags (opens<10%, replies<1%, bounces>3%, canary FAIL). | --period <daily|weekly>, --tmp <path>, --dry-run/--live. Env (live only): TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID. | stdout (dry-run) and .tmp/self_outbound/digest_<date>.txt. |
| `personal_workflows/self_outbound_system/enricher.py` | Enrich leads with real emails (via AnymailFinder) and deliverability verification (via Million Verifier). Dry-run marks every lead verified with a canned score; live mode looks up missing emails then verifies them, dropping catchalls and hard-bounces. | --input <path> (leads json), --dry-run/--live, --min-confidence <int>, --output <path>. Env (live only): ANYMAILFINDER_API_KEY, MILLION_VERIFIER_API_KEY. | .tmp/self_outbound/enriched_leads_<timestamp>.json with {"verified": [...], "unverified": [...], "cost_eur": <float>}. |
| `personal_workflows/self_outbound_system/icp_filter.py` | Filter raw leads against the ICP config. Rejects anti-ICP (equity-only, junior gigs, enterprise procurement, AM-locked domains, etc.), applies signal-count rule, and assigns each kept lead to the best-fitting segment. Deterministic, side-effect-free apart from the output file. | --input <path> (raw leads json), --config <path> (default: config/icp.json), --dry-run. | .tmp/self_outbound/filtered_leads_<timestamp>.json with {"kept": [...], "rejected": [{"lead", "reason"}, ...]}. |
| `personal_workflows/self_outbound_system/instantly_client.py` | Thin CLI wrapper around Instantly.ai Growth REST API v2. Supports upload / stats / pause / resume actions. Dry-run mode issues zero HTTP calls; live mode reads INSTANTLY_API_KEY + INSTANTLY_CAMPAIGN_ID from env. | --input <path> (personalized leads json, required for upload), --action <upload|stats|pause|resume>, --dry-run/--live, --output <path>. Env (live only): INSTANTLY_API_KEY, INSTANTLY_CAMPAIGN_ID. | .tmp/self_outbound/instantly_result_<timestamp>.json with the action + result. |
| `personal_workflows/self_outbound_system/killswitch.py` | One-shot kill switch. --engage sets KILL_SWITCH=1 in the workspace .env and calls instantly_client.py --action pause. --release sets KILL_SWITCH=0 but does NOT auto-resume Instantly (operator must resume in UI + verify canary green). Dry-run prints intent without touching .env or Instantly. | --engage OR --release (mutually exclusive), --dry-run/--live, --env-file <path>. | mutates the workspace .env (live). Prints action + rationale to stdout. |
| `personal_workflows/self_outbound_system/personalizer.py` | Generate per-lead subject + full-body email for cold outreach applying Nick Saraev's 4-step framework (personalization -> who am I -> offer -> CTA). Dry-run uses deterministic mocks drawn from tone.json v2 (with give_first / who_am_i / offer / cta_time_proposals slots). Live mode (STUB) calls Sonnet 4.6 with cached system prompt. Subject/opener are hard-validated against tone.json constraints; leads whose fields fail are refused with a machine-readable reason. | --input <path> (verified leads json), --config <path> (default: config/tone.json), --dry-run/--live, --llm <sonnet|gemini>, --output <path>. Env (live only): ANTHROPIC_API_KEY, GEMINI_API_KEY, CAL_COM_BOOKING_URL. | .tmp/self_outbound/personalized_leads_<timestamp>.json with subject/opener/body_html/body_text/variant/cost_eur_estimate per lead. |
| `personal_workflows/self_outbound_system/reply_classifier.py` | Classify inbound cold-email replies into 5 buckets (hot / positive / neutral / negative / auto_reply_or_OOO). Deterministic pre-filter catches auto-replies + stop-requests before any LLM call; LLM (stubbed in dry-run) handles ambiguous cases. | --reply-body <string> OR --reply-file <path>, --dry-run/--live, --llm <sonnet|gemini>. Env (live only): ANTHROPIC_API_KEY, GEMINI_API_KEY. | JSON to stdout: {"class", "confidence", "explanation", "detected_intent_signals"}. |
| `personal_workflows/self_outbound_system/run.py` | Daily orchestrator for the self-outbound system. Reads KILL_SWITCH first; halts if 1. Chains canary → sourcer → icp_filter → enricher → personalizer → instantly_client(upload) → instantly_client(stats) → acceptance → digest. Canary FAIL halts the day; acceptance FAIL engages the kill switch. | --segment <name>, --limit <int>, --dry-run/--live, --output-log <path>. Env: KILL_SWITCH (halt if 1). | .tmp/self_outbound/run_<date>.log — structured JSONL log of each step. Prints end-of-run digest to stdout. |
| `personal_workflows/self_outbound_system/sourcer.py` | Source raw B2B leads for the self-outbound system. Dry-run returns the frozen 3-lead fixture; live mode calls the Apify HTTP API against harvestapi/linkedin-profile-search (per-segment config in config/apify_sourcing.json) and normalizes the payload into our internal lead schema. | --segment <name>, --limit <int>, --dry-run/--live, --output <path>, --tiny (5-lead safety cap). Env (live only): APIFY_TOKEN. | .tmp/self_outbound/sourced_leads_<timestamp>.json — {leads: [...], dry_run, cost_eur_estimate, sources_used}. |
| `personal_workflows/self_outbound_system/suppression_writer.py` | Append entries to config/suppression.json in a concurrency-safe, dedup-aware, GDPR-compliant way. Callable both as a CLI (single-entry mode) and as a library (bulk-append + import from reply_classifier / webhook_receiver). Every write is idempotent — appending an already-suppressed email is a no-op that still records the event in history. Optional Telegram alert on write. | --email <addr>, --reason <str>, --source <webhook|reply_classifier|manual|cron>, --dry-run/--live, --alert/--no-alert. Env: TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID (optional for alerts). | Updates config/suppression.json. Prints JSON status line to stdout. Exit 0 on success, non-zero on validation failure. |
| `personal_workflows/self_outbound_system/sync_suppression_from_kv.py` | Pull buffered suppression events from the Cloudflare Worker's KV namespace (SUPP_EVENTS), call suppression_writer.add_bulk() to sync them into config/suppression.json, then delete consumed keys from KV. Cron this on the daily pipeline so webhook events land in the local suppression list within hours. | --wrangler-dir <path to worker>, --binding <SUPP_EVENTS>, --dry-run/--live. Env: TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID (optional). | Updates config/suppression.json via suppression_writer. Prints a summary stat line. Exit 0 on success. |
| `personal_workflows/self_outbound_system/tests/conftest.py` | — | — | — |
| `personal_workflows/self_outbound_system/tests/test_icp_filter.py` | Unit tests for icp_filter.py against the frozen 32-case corpus. Every known-bad MUST reject with the correct reason category; every known-good MUST pass. This is the regression-safe layer BENEATH the acceptance gate (which runs the same corpus end-to-end). | — | — |
| `personal_workflows/self_outbound_system/tests/test_personalizer.py` | Dry-run tests for personalizer.py against the 3 seed leads. Assert every subject/opener conforms to tone.json constraints (word counts, banned words, no leaked template tokens). | — | — |
| `personal_workflows/self_outbound_system/tests/test_reply_classifier.py` | Deterministic-layer tests for reply_classifier.py. Every OOO/auto-reply string MUST classify auto_reply_or_OOO without LLM. Every stop/unsubscribe string MUST classify negative. Positive strings are LLM-dependent — marked XFAIL in dry-run. | — | — |
| `personal_workflows/self_outbound_system/tests/test_run_smoke.py` | End-to-end dry-run smoke test. `py run.py --dry-run` MUST exit 0, produce a run log, and emit all pipeline artifacts under .tmp/self_outbound/. | — | — |
| `personal_workflows/self_outbound_system/tests/test_suppression_writer.py` | Unit tests for suppression_writer. Covers happy path (single add), dedup (already-present), history-always-appended, invalid inputs (bad email / reason / source), bulk mode, and concurrency safety via the file lock. Uses --dry-run + a tmp-path monkeypatch so no real writes happen to config/suppression.json. | — | — |
| `personal_workflows/tests/job_search_sheet_front_door.py` | — | — | — |
| `personal_workflows/yoga_jitendra_site/generate.py` | — | — | — |
| `personal_workflows/yoga_jitendra_site/scripts/generate_ambient_loop.py` | — | — | — |
| `personal_workflows/yoga_jitendra_site/scripts/get_google_refresh_token.py` | — | — | — |
| `personal_workflows/yoga_jitendra_site/tests/acceptance_dashboard.py` | LIVE-artifact acceptance gate for the yoga-jitendra dashboard V0.1. | SITE_URL (default https://yogaavecjitendra.fr), DASHBOARD_USER, DASHBOARD_PASS, | exit 0 on full pass, 2 on pass-with-skips (non-strict), 1 on any FAIL. |
| `personal_workflows/yoga_jitendra_site/tests/acceptance_reviews.py` | Hard-fail output-acceptance gate for the yoga_jitendra reviews system. | SITE_URL (default https://yoga-jitendra.pages.dev), DASHBOARD_USER, DASHBOARD_PASS | stdout PASS/FAIL log; exit non-zero on any assertion failure. |
| `personal_workflows/yoga_jitendra_site/tests/e2e_dashboard_auth.py` | Headless-browser end-to-end test for the dashboard auth flow — proves the session-cookie fix eliminates the double-prompt on /dashboard/subscribers/. | SITE_URL (default: https://yogaavecjitendra.fr), DASHBOARD_USER (default: debanjan), DASHBOARD_PASS (required — skipped if unset). --strict to hard-exit on failure. | PASS/FAIL per assertion on stdout; exit 0 on all-pass, 1 on any failure. |
| `personal_workflows/yoga_jitendra_site/tests/e2e_popup.py` | Headless-browser end-to-end test for the NewsletterPopup lifecycle on the LIVE site. | SITE_URL env var (default: https://yogaavecjitendra.fr). --strict to hard-exit non-zero on failure. | PASS/FAIL per assertion on stdout; exit code 0 on all-pass, 1 on any failure. |
| `personal_workflows/yoga_jitendra_site/tests/unit_dashboard.py` | Dist-based structural + regression unit checks for the yoga-jitendra dashboard. | none (static build under dist/ + source tree under src/). | exit 0 on pass, non-zero + failure reasons on stderr. |
| `personalization/ai_opener_generator.py` | Generate personalized cold email first lines using Anthropic or OpenRouter LLM API. | --input, --output, --tone-config, --batch-size, --mock, --mode, --model; | .tmp/personalized_leads.json |
| `personalization/variant_generator.py` | Generate AI challenger email variants and analyze per-variant performance for A/B testing. | --action (generate|analyze|recommend|report), --config, --mode, --model, --mock; | Updated config/email_variants.json, .tmp/variant_report.json |
| `rag/chat.py` | Chat against a RAG store ingested by execution/rag/ingest.py. Free path: | — | — |
| `rag/ingest.py` | Ingest URLs / PDFs / text files into a local RAG vector store. Free-path: | — | — |
| `templates/crm_integration/config/schema.py` | Pydantic-based schema for per-tenant config JSON files. | Path to tenant config JSON, live os.environ | Validated TenantConfig instance, or SystemExit with the offending |
| `templates/crm_integration/destinations/base.py` | — | — | — |
| `templates/crm_integration/destinations/google_sheet.py` | Upsert internal records to a Google Sheet keyed by internal_id. | destination.target = sheet ID; GOOGLE_SERVICE_ACCOUNT_JSON env | rows added / updated in the first worksheet of the target sheet. |
| `templates/crm_integration/destinations/jsonl.py` | Default dev destination. Idempotent by dedup_key. | destination.target = file path | newline-delimited JSON records, plus a sidecar `.seen.json` |
| `templates/crm_integration/mapping.py` | Bidirectional field mapping — provider record <-> internal record. | dict from a provider adapter's list/get_record; internal-shape dict | dict in the opposite shape |
| `templates/crm_integration/provider_adapters/airtable.py` | — | — | — |
| `templates/crm_integration/provider_adapters/attio.py` | — | — | — |
| `templates/crm_integration/provider_adapters/base.py` | Abstract interface every CRM provider adapter implements. | TenantConfig, live os.environ | yields dicts (list_records), returns dicts (get/create/update) |
| `templates/crm_integration/provider_adapters/clickup.py` | — | — | — |
| `templates/crm_integration/provider_adapters/hubspot.py` | List/get/create/update HubSpot contacts + verify HubSpot webhooks. | HUBSPOT_ACCESS_TOKEN (private-app token) in env | dicts matching HubSpot v3 contact shape |
| `templates/crm_integration/provider_adapters/pipedrive.py` | — | — | — |
| `templates/crm_integration/sync.py` | Reads records from a CRM (via provider_adapters/), maps them to | --tenant <slug> (config file at config/tenant.<slug>.json) | JSONL stat line in .tmp/crm_sync_runs/<tenant>.jsonl per run; |
| `templates/crm_integration/tests/integration/test_sync_dry_run.py` | — | — | — |
| `templates/crm_integration/tests/unit/test_config_schema.py` | — | — | — |
| `templates/crm_integration/tests/unit/test_mapping.py` | — | — | — |
| `templates/mobile_app_expo/tests/acceptance_{{APP_SLUG}}.py` | Acceptance gate for {{APP_SLUG}} — hits the paired backend's /api/health and asserts the response contract the mobile app depends on. HARD-FAILS on any deviation. Runs pre-EAS-build in CI (preflight.yml). | API_BASE_URL env var (defaults to http://localhost:8787). Optional --verbose flag. | exit 0 on PASS, exit 1 on FAIL with per-check details on stderr. |
| `templates/scaffold_mobile_app.py` | Scaffold a new mobile app from the workspace-native template at execution/templates/mobile_app_expo/. Copies to C:/Users/deban/dev/mobile-apps/<slug>/ per CLAUDE.md convention (mobile app source lives outside the workspace), replaces {{APP_SLUG}} placeholders in text files, appends an entry to execution/mobile_apps/registry.json, and initializes git in the new repo. | CLI: slug (positional, kebab-case), --dry-run, --force, --backend-stack {cf_modal,supabase} | New repo dir at C:/Users/deban/dev/mobile-apps/<slug>; mutated execution/mobile_apps/registry.json; git repo initialized with one initial commit; printed next-steps checklist. |
| `templates/scaffold_video_pipeline.py` | Scaffold a new video-pipeline project from the template at | — | — |
| `templates/scaffold_web_app.py` | — | — | — |
| `templates/video_pipeline/analyze.py` | Analysis stage of the video pipeline template. Ingests a YouTube URL | — | — |
| `templates/video_pipeline/generate.py` | Generation stage of the video pipeline template. Reads analysis.json | — | — |
| `templates/video_pipeline/publish.py` | Publish stage of the video pipeline template. Takes a finished | — | — |
| `templates/video_pipeline/tests/acceptance_video.py` | — | — | — |
| `templates/video_pipeline/tests/integration/test_pipeline_dry_run.py` | — | — | — |
| `templates/video_pipeline/tests/unit/test_analyze.py` | — | — | — |
| `templates/video_pipeline/tests/unit/test_generate_prompt.py` | — | — | — |
| `templates/web_app_astro_cf/tests/acceptance_PROJECT_NAME.py` | — | — | — |
| `video/prodcraft_creative_critic.py` | World-class critic for ProdCraft creative-plan videos. Renders sample stills | — | — |
| `video/prodcraft_creative_pipeline.py` | End-to-end ProdCraft creative pipeline with internal critique-reroll loop. | — | — |
| `video/prodcraft_creative_plan_gen.py` | GLM 5.2 generates a bespoke per-section CreativePlan. The director call | — | — |
| `video/prodcraft_living_prd_plan.py` | Full Living PRD doc-ops plan for the entire Phase 1 PRD video (~2:27 of v1 audio). Hand-authored, anchored to actual word-timing JSON of phase1_audio_words.json. Six sections cover the script end-to-end with a final outro CTA. | — | — |
| `video/prodcraft_living_prd_plan_gen.py` | Generate a Living PRD doc-ops plan from a script + word timings via Gemini Flash (default), GLM 5.2 (personal mode), or Sonnet 4.6 (client mode). | — | — |
| `video/prodcraft_living_prd_plan_poc.py` | Doc-ops plan generator for the Living PRD POC. Reads the v3 word-timings JSON, finds semantic anchor phrases in the actual transcript, and emits a plan whose ops are aligned to where those phrases land in audio time. Robust against re-renders that shift timestamps. | — | — |
| `video/prodcraft_render.py` | Stage Phase 1 assets into the Remotion project's public/ directory and run `npx remotion render ProdCraftPhase1` to produce the final MP4. Idempotent: overwrites prior staging, never deletes node_modules. | — | — |
| `video/prodcraft_render_living_prd.py` | Stage Living PRD assets into the Remotion project's public/ dir and render the ProdCraftLivingPRD composition. Trims words.json to the POC duration so captions don't overrun. | — | — |
| `video/prodcraft_video_edit_pipeline.py` | Video-to-video editing pipeline (Nick Saraev method). Takes a real | — | — |
| `video/remotion_bootstrap.py` | Scaffold a new Remotion project using the official --three template, apply workspace overlay, junction-symlink node_modules outside OneDrive, and register in registry.json. | CLI: --slug <slug>, --dry-run, --title "...", --fps 30, --width 1920, --height 1080, --duration-frames 900, --force; or --remove <slug> [--purge-cache] | execution/video/remotion-projects/{slug}/, C:/Users/deban/dev/remotion-node-cache/{slug}/node_modules, execution/video/registry.json entry |
| `video/remotion_render.py` | Render a Remotion composition to video via `npx remotion render`. | CLI: --slug <slug> [--composition <id>] [--out <path>] [--frames <range>] | .tmp/remotion-renders/<slug>-<timestamp>.mp4 (or caller-supplied path); prints path, size, render time. |
| `video/youtube_channel_ingest.py` | Ingest a YouTube channel's public uploads + per-video captions via YT Data API v3 + youtube-transcript-api. Two modes — preflight (cheap count + caption-availability map) and ingest (full transcripts + voice-profile bundle). | — | — |
| `video/youtube_video_analyzer.py` | Frame-by-frame breakdown of a YouTube video using PySceneDetect + | — | — |
| `voice_agents/gemini_live_dental_fr/app.py` | Modal-hosted French dental voice receptionist. FastAPI app serves a browser widget at `/` | — | — |
| `voice_agents/retell_dental_poc/create_flow_and_agent.py` | One-shot script to create (or update) a Retell Conversation Flow + Agent for the | — | — |
| `voice_agents/vapi_dental_fr/app.py` | Modal-hosted backend for the Vapi-driven French dental voice receptionist. Serves | — | — |
| `voice_agents/vapi_dental_fr/create_assistant.py` | One-shot script to create (or update) the Vapi assistant for the French dental voice | — | — |
