- [pattern] Nick Saraev v2v method: source-footage-first + trigger+change prompt + parallel-N (5 default) + human-select + 720p seam-cut. Not text-to-video-from-scratch.
- [technical] Higgsfield MCP hosted at `https://mcp.higgsfield.ai/mcp`; installed via `claude mcp add --transport http --scope user`. Requires browser OAuth on next Claude session; tool schemas load only after auth.
- [technical] Higgsfield public tool catalog (from higgsfield.ai/mcp page): video-analyzer, cinematic-image-to-video, motion-control-recast, marketing-video-generator, soul-character-training, viral-clip-generator, personal-clipper, background-image-remover, image-expander, image-reframer, image-upscaler, video-upscaler. No per-tool JSON schemas published — runtime introspection via `ToolSearch("select:mcp__higgsfield__*")` required after OAuth.
- [constraint] Higgsfield ToS URLs (`/terms`, `/tos`, `/terms-of-service`, `/privacy`) all 404 to unauthenticated fetchers 2026-07-07. Operator owes a manual read on next Higgsfield login BEFORE uploading real client footage.
- [technical] Sensitivity routing: Chinese-jurisdiction models (Kling, Wan, Seedance, Hailuo) BLOCKED at parse-time when `--sensitivity sensitive` because client footage often contains real people's faces. US-jurisdiction models only for sensitive: Gemini Omni, Runway Gen-3, Sora 2, Veo 3.1.
- [technical] Consent gate is presence-only: script checks file exists + non-empty + logs SHA + mtime. Content is NOT validated (by design; operator attests fitness). SHA+mtime enables post-hoc stale-release audit.
- [pattern] Cost math: Nick's $0.50-$1 per candidate × 5 candidates = $2.50-$5 per accepted scene. In EUR: €4.60/candidate for Gemini Omni interactive, €0.28 for Kling. Per-invocation gate at $2; daily rolling warn at $5.
- [technical] `--dry-run` mode fully validates schema + gates + cost + emits manifest.json with $0 spend. `--live` is stubbed with LIVE_MODE_NOT_YET_WIRED until Higgsfield MCP OAuth is complete.
- [constraint] Rule 5: source clips MUST be ≤10s. ffprobe check enforced unless `--skip-duration-check`. Long-form via chained ≤10s clips is Phase 7 (`prodcraft_longform_chain.py`), not currently built.
- [technical] Seam-check enforcement is a LINT (`--check-plan <plan.json>`), not automated frame-analysis. PySceneDetect-based enforcement is Phase 8, triggered when acceptance-corpus > 10 triplets OR first client complains.
