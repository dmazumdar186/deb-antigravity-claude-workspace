# Notes

- [technical] LinkedIn guest API geoIds verified live 2026-09-06 for FR, DE, AT, BE, NL, GB, CH, IN, SG, CA; US relies on a state-abbreviation regex in registry.py because guest results use "City, ST" strings.
- [learned] A live `preview` for a Sales Manager FR+DE profile fetched 345 jobs in ~5 min (LinkedIn FR 93, DE 96, WTTJ FR 104, Hellowork 35, RemoteOK 17; France Travail 0 without keys) and kept 13 relevant rows; run preview at most once per hour.
- [learned] A bare "Remote" location was classified HYBRID by the ported remote detector, silently dropping every RemoteOK/WeWorkRemotely job — location string is now checked before description text.
- [learned] Marking jobs seen before a confirmed email send burns jobs on the second daily cron fire; state is marked only for digest rows after a real SMTP send.
- [pattern] Acceptance gate must be an independent implementation (own tokenizer/alias match), otherwise it is a tautology of the filters and exit 3 is dead.
- [constraint] The friend's repo layout is engine/job_digest + engine/requirements.txt + PYTHONPATH=engine; the shipped zip excludes tests/ so fixtures live in job_digest/fixtures/.
- [constraint] A Claude subscription does not provide an ANTHROPIC_API_KEY; unattended runs default to the heuristic ranker.
- [pattern] scripts/package_job_digest.py refuses to build if any bundled file contains operator personal data (blocklist scan) — keep it that way for anything shared outside the workspace.
- [technical] geoId re-verification table, 2026-09-06 (keyword "manager", majority of sampled locations in-country): FR 105015875, DE 101282230, AT 103883259, BE 100565514, NL 102890719, GB 101165590, CH 106693272, IN 102713980, SG 102454443, CA 101174742 all verified live; US 103644278 verified by inspection (Buffalo NY, Springfield IL, Lafayette LA) and handled via the state-abbreviation regex, not a country alias/geoId spot-check.
- [learned] Live end-to-end self-test on GitHub Actions (2026-09-06, run 34032741949, workflow .github/workflows/job_digest_selftest.yml): 390 fetched (LinkedIn FR 92 / DE 91, WTTJ FR 80, France Travail 93, Hellowork 26, RemoteOK 4, WWR 4), 91 kept, 30 emailed, 4 Sheet tabs written to a throwaway sheet shared with the service account, state committed back to the branch. Runtime ~5 min. Gemini's 40-job chunk truncated at 8k output tokens → chunks are now 20 with a 32k budget, thinking disabled, and per-chunk fallback. Anthropic rung fails on the console's €0 balance and degrades cleanly.
- [constraint] Dispatching a brand-new workflow by filename returns 404 until GitHub has indexed it (first run via a push trigger); after that `workflow_dispatch` works on the branch.
- [technical] LinkedIn guest detail enrichment returns 429 bursts on the GitHub runner; ~70% of cards still get JD text. Search pages themselves were not blocked.
- [learned] Second live self-test (2026-09-06 13:36 UTC, run 34036599844): 386 fetched, 308 new after cross-day dedup (26 already seen), 48 kept, 30 emailed, sheet_ok, state pushed. Gemini rung "ran" on the full batch with 20-job chunks: one chunk hit 503/504 "high demand" after 3 retries, the split-in-half fallback recovered both halves — no heuristic-only rows. Gemini free tier is flaky under load; the fallback is load-bearing, keep it.
