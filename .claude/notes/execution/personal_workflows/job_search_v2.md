- [learned] 2026-06-16 drought diagnosis: "no new jobs today" was NOT a bug. First run at 18:06:04 fetched 8 (FT:4 + LinkedIn:4), pushed 7 to sheet. Subsequent runs hit already_seen:8 → after_dedup_new:0. Dedup is working correctly. The real signal was thin live coverage: indeed_gmail=0 (empty label), wttj=0 + apec=0 (fixture-only).
- [pattern] When the daily count drops, check `.tmp/job_search_v2/run_log.jsonl` — the per_source counts + dedup stats answer "starved or duplicated" in one line per run. No need to spelunk into seen.db unless those counts already look healthy.
- [pattern] Adding more sources (hellowork_gmail, jobgether_gmail) widens the live funnel without scraping risk because the Gmail-alert pattern reuses GMAIL_SMTP_USER/GMAIL_SMTP_APP_PASSWORD and per-source try/except in run.py prevents cascade failure.
- [constraint] Parser regexes in hellowork_gmail.py / jobgether_gmail.py are heuristic until a real alert email is saved to tests/fixtures/{site}_email_sample.html. The "parsed 0 jobs from email" warning is the canary if the heuristic misses real layout — surface it in the digest, not just logs.
- [technical] indeed_gmail / linkedin_gmail / hellowork_gmail / jobgether_gmail wrappers in run.py all ignore `max_pages`. Symmetric by design (Gmail labels paginate by --days, not pages). The Pyright "unused-parameter" hint is intentional.