---
description: Regenerate the execution registry after adding or modifying scripts
---

Run the registry generator:

```bash
python3 execution/generate_registry.py
```

Then read `execution/REGISTRY.md` to confirm the new/updated script appears correctly. Report any scripts that are missing or have stale descriptions.
