---
description: Read the execution registry before writing any new script
---

Read `execution/REGISTRY.md` in full. Then list:
1. Any existing scripts that could handle the current task (or be extended to do so)
2. Any gaps where a new script would be needed

Do not write any new code yet — just report what exists and what's missing.
